function MarketJob() {
    const SELF = {

    }

    return {
        getStockPrice: async (dateStr) => {
            try {

            } catch (err) {
                console.error(`Error getting stock price for ${dateStr}:`, err);
            }
        }
    }
}